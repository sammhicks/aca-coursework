var a[3]
var b[3]

for {var i = 0} ; {var cmp = i <> 3; cmp<} ; {i = i + 1} {
  a[i] = [0,10)
  b[i] = [0,10)
}

var sum = 0

for {var i = 0} ; {var cmp = i <> 3; cmp<} ; {i = i + 1} {
  var v = a[i]
  sum = sum + v
  v = b[i]
  sum  = sum + v
}
