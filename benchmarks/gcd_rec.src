
var a = 16
var b = 24

var r
gcd(a, b) -> r

gcd(a, b) -> r {
  var cmp = a<>b
  if {cmp=} {
    r = a
    return
  } else if {cmp<} {
    a = a - b
    gcd(a, b) -> r
    return
  } else {
    b = b - a
    gcd(a, b) -> r
    return
  }
}
